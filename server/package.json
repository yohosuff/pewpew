{
    "name": "bishop",
    "private": "true",
    "version": "0.0.1",
    "license": "MIT",
    "main": "dist/index.js",
    "scripts": {
        "prebuild": "tslint -c tslint.json -p tsconfig.json --fix",
        "build": "tsc",
        "prestart": "npm run build",
        "start": "node ."
    },
    "dependencies": {
        "express": "^4.17.1",
        "socket.io": "^4.0.2"
    },
    "devDependencies": {
        "@types/express": "^4.17.11",
        "tslint": "^6.1.3",
        "typescript": "^4.2.4"
    }
}
